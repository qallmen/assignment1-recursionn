name: ILiyas Kalmen 
SE_2514 
<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/ec8f3c10-b496-42ed-8308-2ba6fa33875c" />
<img width="1920" height="1080" alt="image" src="https://github.com/user-attachments/assets/4a498738-6755-417e-a1bf-1c8fb2be1261" />
Part 1:
task1: Create a recursive function that breaks down an integer and prints each of its digits on a new line.
task2: Use recursion to sum the elements of an array and then divide that sum by the count to find the average.
task3:  Implement a recursive test to determine if a number $n$ has any divisors other than 1 and itself
task4: : Write a recursive function to calculate the product of all positive integers up to n 
task5:  Find the $n$-th number in the Fibonacci sequence using the recursive formula F_n = F_{n-1} + F_{n-2}.
task6:  Create a recursive method to calculate the value of a number a raised to power of n 
task8: Recursively examine each character of a string to verify if the entire string consists only of numeric digits.
task9: Determine the total length of a string by recursively counting its characters one by one.

